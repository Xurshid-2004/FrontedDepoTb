name: tb_app
description: TB — Texnika xavfsizligi va Omborxona tizimi (Buxoro lokomotiv deposi, TCH-6)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^2.6.1
  go_router: ^14.6.2
  http: ^1.2.2
  shared_preferences: ^2.3.3
  flutter_secure_storage: ^9.2.2
  camera: ^0.11.0+2
  local_auth: ^2.3.0
  signature: ^5.5.0
  qr_flutter: ^4.1.0
  intl: ^0.19.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
