# TB loyihasi — hisobot va master prompt
**Sana:** 06.08.2026 · **Versiya:** 1.0

---

## 1. Bugun bajarilgan ishlar

### Hujjatlar
| Fayl | Mazmuni |
|---|---|
| `tbsuratlari/TB_TZ_v1.0.docx` | Toʻliq texnik topshiriq — 24 sahifa, 31 boʻlim |
| `tbsuratlari/TB_KONTEKST_2026-08-06.md` | Loyiha konteksti va qarorlar |
| `tb_web/SETUP.md` | Bosqichma-bosqich ishga tushirish qoʻllanmasi |
| `tb_web/app_flutter/README.md` | Android ilova qoʻllanmasi |

### Baza
- `supabase/migrations/0001_init.sql` — **28 jadval**, 8 enum, RLS siyosatlari, triggerlar, `v_worker_norm_status` koʻrinishi, boshlangʻich depo + 3 kolonna + sozlamalar.
- Muhim: `item_variants` jadvali — oʻlcham boʻyicha alohida qoldiq (48/50/52, poyabzal 41/42/43).

### Backend (Next.js API)
| Endpoint | Vazifa |
|---|---|
| `GET /api/me` | Foydalanuvchi, normalari, holati (yashil/sariq/qizil), talon, imtixon, KIP |
| `GET/POST /api/requests` | Arizalar; yaratishda norma + muddat + mavsum tekshiruvi |
| `POST /api/requests/:id/action` | tasdiq · rad · berdim · oldim (state machine) |
| `GET/POST /api/stock` | Qoldiq va kirim (nakladnaya bilan) |
| `GET/POST /api/journals/1\|2` | Yo D-26 jurnallari (ikkalasi mustaqil) |
| `POST /api/sign` · `GET /api/verify/:id` | ECDSA P-256 QR imzo va tekshiruv |
| `GET /api/documents/:turi/:id` | Требование, MB-6, jurnal — PDF |
| `POST /api/auth/face` | Face ID tekshiruvi (tabel + F.I.Sh. + 3 kadr) |
| `POST /api/admin/seed` | 31-ilova maʼlumotlari |
| `GET /api/health` | Monitoring |

### Hujjat generatsiyasi (PDF)
- `lib/pdf.ts` — DejaVu Sans embed (kirill + oʻzbek lotin), jadval, QR imzo bloki.
- `lib/docs/requisition.ts` — **Требование Форма МУ№27**, 20 maydon, 4 QR imzo.
- `lib/docs/card.ts` — **MB-6** old (01–20) va orqa (Berilgan 21–25, Qaytarilgan 26–30).
- `lib/docs/journal.ts` — **Yo D-26**, albom, koʻp qatorli kataklar, avtomatik sahifalash.
- Uchalasi sinovdan oʻtkazildi, chiqish tekshirildi.

### Face servis
- `face-service/app.py` — FastAPI + InsightFace buffalo_l, `/embed` · `/compare` · `/verify` (jonlilik tekshiruvi bilan).
- `Dockerfile` — model image ichiga oldindan yuklanadi.

### Deploy
- `Dockerfile` (Next.js standalone, 3 bosqichli), `docker-compose.yml` (web + face + Caddy), `Caddyfile` (avtomatik TLS), `deploy.sh` (Ubuntu uchun bir buyruqli oʻrnatish).

### Android ilova (Flutter)
- `app_flutter/` — kirish oqimi (tabel → F.I.Sh. → Face ID → imzo → PIN → barmoq izi), ishchi kabineti, ariza yuborish, arizalar holati.

### Tekshiruv
- TypeScript: **xatosiz** (`tsc --noEmit`).
- PDF: uchala hujjat yaratildi va koʻz bilan tekshirildi.
- `next build` sinov muhitida resurs cheklovi sababli tugamadi — oʻz kompyuteringizda `npm run build` bilan tasdiqlash kerak.

---

## 2. Qabul qilingan yangi qarorlar (06.08.2026)

1. **«Навбатчи» buyumlar** tizimdan butunlay chiqarildi.
2. **Rad etish izohi majburiy** — depo boshligʻidan tashqari barcha rollar uchun.
3. **Qishki mavsum**: 15-sentabr – 15-aprel (admin sozlaydi).
4. **Kolonnalar**: admin biriktiradi — El. mashinist/yordamchi, Teplovoz mashinist/yordamchi, Manyovr teplovoz mashinist/yordamchi (yana qoʻshiladi).

---

## 3. Menga toʻsiq boʻlgan narsalar (faqat siz qila olasiz)

| № | Nima | Nega men qila olmayman |
|---|---|---|
| 1 | Supabase loyihasi ochish | Hisob yaratish va parol talab qilinadi |
| 2 | Server sotib olish | Toʻlov karta bilan amalga oshiriladi |
| 3 | Domen sotib olish | Toʻlov |
| 4 | GitHub repo va Vercel ulash | Hisobga kirish |
| 5 | Android Studio + Flutter SDK oʻrnatish | Sizning kompyuteringizda |
| 6 | Kadrlar boʻlimidan ishchilar roʻyxati va yuz suratlari | Tashkiliy masala |

---

## 4. Server tavsiyasi

| Variant | Narx | Izoh |
|---|---|---|
| **Hetzner CX22** (2 vCPU, 4 GB, 40 GB) | ~€4/oy | Web + Face servis uchun yetarli, Germaniya |
| **Hetzner CX32** (4 vCPU, 8 GB, 80 GB) | ~€7/oy | Bemalol, InsightFace tezroq ishlaydi — **tavsiya** |
| Contabo VPS S | ~€6/oy | Arzon, lekin sekinroq |

Server olingach mendan faqat IP va SSH kirish kerak — qolganini `deploy.sh` bajaradi.

---

## 5. Ishga tushirish tartibi (siz boʻsh boʻlganingizda)

```powershell
cd "C:\Users\ANUBIS PC\Desktop\tb_web"
```
```powershell
npm install @supabase/supabase-js pdf-lib @pdf-lib/fontkit
```
```powershell
npm run build
```

Soʻng `SETUP.md` dagi 2–6 qadamlar: Supabase → `.env.local` → seed → birinchi admin.

---

# MASTER PROMPT — keyingi suhbat uchun

> Men Oxun. TB loyihasi ustida ishlayapmiz — «TEMIRYOʻLINFRATUZILMA» AJ, Buxoro lokomotiv deposi (TCH-6) uchun Texnika xavfsizligi va Omborxona tizimi.
>
> **Til:** faqat oʻzbek lotin, kirill aralashmaydi (`oʻ`, `gʻ`). Kod va terminal buyruqlari inglizcha.
> **Muhit:** Windows + PowerShell, `&&` ishlamaydi — har bir buyruq alohida.
> **Uslub:** avval reja → tasdigʻimni soʻra → keyin kod. Bir vaqtda bitta qadam. Toʻliq fayl mazmuni ber, qismlar emas. Ortiqcha qoʻshimcha qoʻshma. Noaniq boʻlsa — soʻra, faraz qilma.
>
> **Loyiha papkalari:**
> - `C:\Users\ANUBIS PC\Desktop\tb_web` — Next.js web + API + Docker + Flutter ilova (`app_flutter/`)
> - `C:\Users\ANUBIS PC\Desktop\tbsuratlari` — blanka rasmlari, TZ va kontekst hujjatlari
>
> **Birinchi qadam:** `tbsuratlari\TB_TZ_v1.0.docx` va `tb_web\HISOBOT_2026-08-06.md` fayllarini oʻqi — loyihaning toʻliq holati oʻsha yerda.
>
> **Arxitektura:** Flutter (Android/iOS/Web) → Next.js API → Supabase (PostgreSQL/Auth/Storage) + Face servis (Python/InsightFace, Docker) + QR imzo (ECDSA P-256). Multi-depo boshidan. Soft-delete — hech narsa oʻchirilmaydi.
>
> **Tayyor:** SQL sxema (28 jadval), API qatlami, PDF generatsiya (Требование МУ№27, MB-6, Yo D-26), Face servis, Docker/Caddy deploy stack, Flutter ilova skeleti, TZ hujjati.
>
> **Qolgan:** Supabase ulash, server olish va deploy, rahbariyat ekranlari (tasdiqlash/ombor/jurnal) mobil versiyada, push (FCM), boshlangʻich maʼlumot importi, frontend dizayn namunalari asosida yakunlash.

---

## 6. Hali javob kutayotgan savollar

1. Smartfoni boʻlmagan ishchilar uchun ombor yonida kiosk rejimi kerakmi?
2. Chop etish: qogʻoz formati (A4/A5), printer, nusxa arxivda saqlanadimi?
3. Ombor bittami yoki sex omborlari ham bormi?
4. Buyum yoʻqotilsa/buzilsa maoshdan ushlab qolinadimi?
5. Yillik inventarizatsiya moduli kerakmi?
6. Depoda internet uzilganda offline rejim kerakmi?
